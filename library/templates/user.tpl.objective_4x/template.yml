kind: mechanical_template
id: user.tpl.objective_4x
version: 0.1.0
class: fixed
description: housing for user.lens.objective_4x (attached from BUY - Objectiv 4x - XXX__mit Gehäuse.stp)
tags:
  - bound
# Declared in the frame the mesh-pose grid below produces: the housing runs
# along z, the optical axis, matching the ports. The old values were the raw
# file-axis box (long axis x), which the declared grid re-maps.
envelope:
  x-mm: 34.989
  y-mm: 35
  z-mm: 61
step: BUY - Objectiv 4x - XXX__mit Gehäuse.stp
glb: BUY - Objectiv 4x - XXX__mit Gehäuse.glb
mesh-frame: record
mesh-pose:
  rotation:
    type: grid
    grid:
      z: -y
      x: +z
optical_ports:
  front:
    frame: optical
    direction: +z
  back:
    frame: optical
    direction: -z
footprint_grid: null
insert-pose:
  rotation:
    type: grid
    grid:
      z: -z
      x: -x
  translation:
    offset-mm: {}
component: user.lens.objective_4x@^0.1
frames:
  optical:
    x-mm: 0
    y-mm: 0
    z-mm: 0
