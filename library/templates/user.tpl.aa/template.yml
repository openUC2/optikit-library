kind: mechanical_template
id: user.tpl.aa
version: 0.1.0
class: fixed
description: mount for user.source.aa (bound from spacer_endstop.step)
tags:
  - bound
envelope:
  x-mm: 50
  y-mm: 50
  z-mm: 50
step: spacer_endstop.step
glb: spacer_endstop.glb
mesh-offset:
  x-mm: 10
  y-mm: -5
  z-mm: -11
  rot-deg:
    x: 0
    y: 0
    z: 0
optical_ports:
  out:
    frame: out
    direction: +x
footprint_grid:
  - 1
  - 1
  - 1
review:
  - carried over from the pre-reset library — the prescription has NOT been confirmed against a real
    part. Verify it in the component editor and delete this note before using it in a design you 
    intend to build.
