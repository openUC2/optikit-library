# The starter camera — copy this record when authoring a detector (WP-68).
# Detectors are fragment-less: the record's whole optical statement is the
# `sensor` port, facing −z so the beam ENTERS the sensor plane (the chain
# terminates here — WP-29 port conventions).
kind: optical_component
id: openuc2.detector.camera_basic
version: 0.1.0
category: detector
description: Starter CMOS camera (sensor plane at the cube center) — copy for detectors
tags: [starter, detector, detector/camera]
optics:
  frames:
    optical: {z-mm: 0.0}
  ports:
    sensor: {frame: optical, direction: -z}
