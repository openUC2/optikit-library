# The starter motorized z-stage — copy this trio when authoring an actuated
# part (WP-68). The component itself is non-optical (WP-30: no optics block,
# BOM identity only); the motion lives on the TEMPLATE as a `dz` DOF and the
# MODULE binds that DOF to firmware (the WP-42 actuation contract).
kind: optical_component
id: openuc2.stage.z_motor
version: 0.1.0
category: stage
description: Starter motorized z-stage — the exemplary actuated (T2) trio to copy
tags: [starter, stage, stage/z]
