# Non-optical component (WP-30): the UC2e ESP32 controller board.
# Electronics records carry vendor/BOM identity and the firmware contract
# reference — no optics block; chain inference and optics-DRC skip them.
kind: optical_component
id: openuc2.electronics.uc2e_esp32
version: 1.0.0
category: electronics
description: UC2e ESP32 controller board (CAN master, WiFi/USB, UC2-REST firmware)
tags: [electronics, controller, esp32]
vendor:
  name: openUC2
  mpn: UC2E-ESP32-V3
  url: https://openuc2.com
electronics:
  firmware: uc2-esp32 (UC2-REST)
  transport: can
review:
  - carried over from the pre-reset library — the prescription has NOT been confirmed against a real
    part. Verify it in the component editor and delete this note before using it in a design you 
    intend to build.
