kind: optical_component
id: openuc2.lens.tube_lens_1x1
version: 0.2.1
category: lens
description: Infinity‑corrected tube lens for microscope systems.
tags:
  - migrated
  - lenses
docs: []
review:
  - auto-migrated from the legacy CSV palette (WP-43) — confirm the record
  - lens surfaces are a thin biconvex approximation from focalLength — verify the trace
optics:
  frames:
    optical:
      z-mm: 0.0
    # Exit vertex = first vertex + center thickness. The migration had `back`
    # on the optical frame, which placed surface 1 at z = 0 and collapsed the
    # glass span to zero thickness (a degenerate self-intersecting body).
    exit:
      z-mm: 3.0
  ports:
    front:
      frame: optical
      direction: -z
    back:
      frame: exit
      direction: +z
      after-surface: 1
  fragment-file: optics.optiland.yml
effective_focal_length_mm: 200.0
