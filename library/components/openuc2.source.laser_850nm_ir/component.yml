# WP-44: the focus-lock probe — a collimated IR diode (~850 nm) whose
# reflection off the coverslip tracks focus (docs.openuc2.com focus-lock).
kind: optical_component
id: openuc2.source.laser_850nm_ir
version: 0.1.0
category: source
description: Collimated IR laser diode (~850 nm) for the focus-lock probe
tags: [source, ir, focus-lock]
review:
  - authored for the focus-lock group (WP-44) — confirm diode specs
optics:
  frames:
    optical: {z-mm: 0.0}
  ports:
    out: {frame: optical, direction: +z}
source:
  wavelengths_um: [0.85]
  divergence_deg: 0.5
