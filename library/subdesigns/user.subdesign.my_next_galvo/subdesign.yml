kind: cube_subdesign
id: user.subdesign.my_next_galvo
version: 0.1.0
description: user.mirror.my_next_galvo decomposed — sliders ride inputs tilt_x, tilt_y
component: user.mirror.my_next_galvo@^0.1
tags:
  - subdesign
  - decomposed
review:
  - "optics-only children: the module's template still renders the monolithic mesh, so the beam scans
    while the CAD mirror does not turn — split the mesh with `optikit-core library decompose` when the
    moving subtrees should follow"
