kind: cube_module
id: user.cube.testlens
version: 0.1.0
description: testlens in a 1x1 cube (bound)
tags:
  - bound
component: user.lens.testlens@^0.1
template: user.tpl.testlens@^0.1
footprint_grid:
  - 1
  - 1
  - 1
review:
  - carried over from the pre-reset library — the prescription has NOT been confirmed against a real
    part. Verify it in the component editor and delete this note before using it in a design you 
    intend to build.
