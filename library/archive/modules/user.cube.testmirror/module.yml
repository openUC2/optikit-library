kind: cube_module
id: user.cube.testmirror
version: 0.1.0
description: testmirror in a 1x1 cube (bound)
tags:
  - bound
component: user.mirror.testmirror@^0.1
template: user.tpl.testmirror@^0.1
footprint_grid:
  - 1
  - 1
  - 1
review:
  - carried over from the pre-reset library — the prescription has NOT been confirmed against a real
    part. Verify it in the component editor and delete this note before using it in a design you 
    intend to build.
