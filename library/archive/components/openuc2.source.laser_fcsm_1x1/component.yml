kind: optical_component
id: openuc2.source.laser_fcsm_1x1
version: 0.1.0
category: source
description: Single‑mode diode laser with FC/APC fiber output for stable, low‑noise delivery; ideal 
  for excitation and alignment.
tags:
  - migrated
  - fluorescence
docs: []
review:
  - auto-migrated from the legacy CSV palette (WP-43) — confirm the record
optics:
  frames:
    optical:
      z-mm: 0.0
  ports:
    out:
      frame: optical
      direction: +z
      # WP-46: this laser delivers through an FC/APC connector, so its output
      # couples only via a declared fiber — never straight into free space.
      coupling: fiber
