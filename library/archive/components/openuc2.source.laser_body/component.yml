# WP-83 S2 reference: a bare laser BODY — a housed device with no cube
# (WP-67). The symbol is this fragment-less source record (emission at the
# housing front face, z = +50); the body is openuc2.tpl.laser_body. power-mw
# feeds the WP-74 photon budget.
kind: optical_component
id: openuc2.source.laser_body
version: 0.1.0
category: source
description: Collimated laser module in its own cylindrical body — the S2 bare-housing reference
tags: [reference, source, source/laser]
optics:
  frames:
    optical: {z-mm: 0.0}
    emission: {z-mm: 50.0, clear-aperture-mm: 10.0}
  ports:
    out: {frame: emission, direction: +z}
source:
  wavelengths_um: [0.635]
  divergence_deg: 0.5
  power_mw: 1.0
review:
  - carried over from the pre-reset library — the prescription has NOT been confirmed against a real
    part. Verify it in the component editor and delete this note before using it in a design you 
    intend to build.
